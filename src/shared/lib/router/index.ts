import { createRoute } from "atomic-router";

export const routes = {
    home: createRoute(),
    about: createRoute(),
    contacts: createRoute(),
    articleList: createRoute(),
    article: createRoute(),
    blog: createRoute(),
}
